<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wg.extend.mybatis.entity.mapper.DemoMapper">
    <select id="getById" resultType="com.wg.extend.mybatis.entity.Demo">
           select * from demo where id = ?
    </select>

    <select id="getAll" resultType="com.wg.extend.mybatis.entity.Demo">
           select * from demo
    </select>
</mapper>